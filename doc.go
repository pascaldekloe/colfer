// Package colfer generates code.
package colfer

const (
	// Magic is the header byte for data structure serials.
	Magic = 0x80

	// MIMEType is the (unregistered) media type identifier.
	MIMEType = "application/colfer"
)
