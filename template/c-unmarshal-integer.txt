	word_t v{{.Index}} = word{{.WordIndex}} >> ({{.WordShift}} + 1) & 0x7f;
	if (((uint64_t)1 << {{.WordShift}} & word{{.WordIndex}}) == 0) {
		int tail_size = __builtin_ctz(v{{.Index}} | 0x80) + 1;
		word_t tail;
		memcpy(&tail, p, 8);
		p += tail_size;
		v{{.Index}} <<= (tail_size << 3) - tail_size;
		v{{.Index}} |= tail & COLFER_MASKS[tail_size];
	}
{{- /* no trailing new-line */ -}}
